WHO OWNS WHICH FOLDER
Do not edit another person's folder.

Lead (Cursor) owns:
- app/page.tsx (login)
- app/artisan/
- app/buyer/
- app/chat/
- app/map/
- data/starter-list.json
- lib/
- components/ (shared UI: AppHeader, LoginForm, NeedLogin, ChatPanel, PoolCard, ScoreBadge, FirozabadMap, …)
- README.md

Person A (Antigravity) owns:
- app/phone/

Person B (Antigravity) owns:
- app/matching/

Person C (Antigravity) owns:
- app/pickup/
- app/money/

Shared keys (do not fork):
- localStorage `kanch-person-c-batches` — Person C writes QC; lead `lib/store` + pool/score read it
- localStorage piles/demands/chat/pools — lead `lib/store.ts`

Before you work: Pull (see team-guide/09-CHATGPT-ZERO-SETUP.txt).
After you work: Push only your folder.
If GitHub fights you: stop and message the lead.
